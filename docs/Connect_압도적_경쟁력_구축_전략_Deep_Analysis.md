# Connect의 압도적 경쟁력 구축 전략: Deep Analysis

> **분석 목적:** Connect가 경쟁자 대비 복제 불가능한 경쟁우위를 구축하기 위한 전략 프레임워크 및 실행 로드맵
> **분석 관점:** SaaS 백엔드/PostgreSQL(DBA) 30년 + Netflix/YouTube 추천시스템 5년 경험 기반
> **핵심 전제:** PDF 추출, DB 구축, 기본 매칭은 모두 경쟁자가 돈·시간·인력으로 복제 가능 → 진짜 경쟁우위는 다른 곳에 있어야 함

---

## 🔍 1단계: 문제 진단 — "경쟁우위는 어디에 없는가"

먼저 **모방 가능한 영역**을 명확히 합니다:

| 기술 요소 | 모방 난이도 | 모방 소요 시간 | 결론 |
|----------|-----------|-------------|------|
| PDF → 구조화 데이터 추출 (OCR + LLM) | 🟢 쉬움 | 1~2개월 | ❌ **Moat 아님** |
| 공고 DB 구축 (크롤링 + 정규화) | 🟢 쉬움 | 2~3개월 | ❌ **Moat 아님** |
| 키워드 매칭 알고리즘 | 🟡 보통 | 3~6개월 | ❌ **Moat 아님** |
| 사용자 프로필 DB | 🟡 보통 | 3~6개월 | ❌ **Moat 아님** |

**Netflix의 교훈:** Netflix의 추천 시스템이 강력한 이유는 알고리즘이 뛰어나서가 아닙니다. 2006년 Netflix Prize에서 $1M을 걸고 알고리즘 공모를 했을 때, 우승 알고리즘은 겨우 10% 개선을 달성했습니다. **진짜 경쟁우위는 알고리즘이 아니라, 알고리즘이 학습하는 데이터의 양과 질, 그리고 피드백 루프의 속도**였습니다.

### ★ Insight
- **"경쟁자가 돈·시간·인력으로 복제할 수 있는 것은 경쟁우위가 아닙니다."** — 이것이 30년간 빅테크에서 얻은 제1원칙입니다.
- PDF 추출, DB 구축, 기본 매칭은 모두 **"Table Stakes"**(최소 진입 요건)이지, 차별화 요소가 아닙니다.
- Connect의 진짜 경쟁우위는 **아직 존재하지 않으며, 의도적으로 설계하고 구축해야 합니다.**

---

## 🏗️ 2단계: 경쟁우위 4-Layer 프레임워크

Netflix/YouTube/Google에서 검증된 **"Defensibility Ladder(방어벽 사다리)"** 프레임워크를 Connect에 적용합니다:

```
Layer 4: 네트워크 효과 + 전환비용     ← 🔒 난공불락 (3~5년 후)
Layer 3: 독점 성과 데이터 + 피드백 루프  ← 🛡️ 강한 방어벽 (1~2년 후)
Layer 2: 도메인 특화 지식 그래프       ← 🧱 중간 방어벽 (6개월~1년)
Layer 1: 구조화 데이터 + 기본 매칭     ← 🏠 기초 (현재)
```

---

### 🏠 Layer 1: 구조화 데이터 + 기본 매칭 (현재 수준)

**현황:** 공고 크롤링 → DB 저장 → 키워드 기반 매칭
**문제:** 누구나 3개월 안에 복제 가능

**그러나 Layer 1을 "싸고 빠르게" 완성하는 것은 필수입니다.** 여기서 비용을 최소화하는 전략:

#### PDF 추출 비용 최적화 전략:

```
현재 접근: 매 PDF → Claude Opus 4.5 API ($5/MTok input, $25/MTok output)
  ※ 참고: Legacy Opus 4/4.1은 $15/$75이며, Opus 4.5는 3배 저렴
문제: 전량 Opus 사용 시 비용 과다 + 속도 느림

권장 접근: 3-Tier 추출 파이프라인
┌────────────────────────────────────────────────┐
│  Tier 1: 규칙 기반 추출 (비용: $0)              │
│  - 공고번호, 기관명, 날짜, 금액 등 정형 필드     │
│  - 정규식 + 템플릿 매칭으로 80% 필드 추출        │
│  - 정부부처별 PDF 포맷이 상당히 정형화되어 있음   │
├────────────────────────────────────────────────┤
│  Tier 2: 경량 LLM 추출 (비용: ~$0.003/공고)     │
│  - 연구내용, 자격요건 등 반정형 필드              │
│  - Claude Haiku 또는 로컬 모델 사용              │
│  - Tier 1에서 놓친 20% 필드 처리                 │
├────────────────────────────────────────────────┤
│  Tier 3: 전문가 검증 + Opus (비용: 선별적)       │
│  - 신규 포맷, 복잡한 자격요건 등 예외 케이스만    │
│  - 전체의 5% 미만에만 적용                       │
│  - 동시에 Tier 1/2의 학습 데이터로 환류          │
└────────────────────────────────────────────────┘
```

**예상 비용:** 공고 1건당 평균 ~$0.005 (Opus 4.5 전량 사용 시 ~$0.15 추정 대비 **약 1/30 비용**)
> ※ ROI 산정 근거: 평균 공고 PDF ~15K tokens input + ~5K tokens output 기준
> - Opus 4.5 전량: ($5×0.015) + ($25×0.005) = $0.075 + $0.125 = ~$0.20/공고
> - 3-Tier 파이프라인: Tier 1($0) 80% + Tier 2(Haiku ~$0.003) 15% + Tier 3(Opus ~$0.20) 5% = ~$0.01/공고

---

### 🧱 Layer 2: 도메인 특화 지식 그래프 — **첫 번째 진짜 경쟁우위**

이것이 **경쟁자가 6개월~1년 걸려도 따라잡기 어려운** 첫 번째 방어벽입니다.

#### 핵심 개념: "공고 텍스트" 너머의 구조화된 지식

경쟁자가 PDF에서 추출하는 것: **텍스트 데이터**
Connect가 구축하는 것: **관계형 지식 그래프(Knowledge Graph)**

```
예시: "반려동물난치성질환극복기술개발사업" 공고로부터 추출 가능한 지식

[텍스트 수준] (경쟁자도 가능)
  "악성종양 치료제 개발, 세포·유전자·면역치료 기술..."

[지식 그래프 수준] (Connect만 구축)
  사업 ─관할→ 농림축산식품부
      ─전문기관→ 농림식품기술기획평가원
      ─역사적패턴→ 2024년 유사사업 선정률 23%
      ─평가위원풀→ 수의학 + 바이오 + AI 전문가
      ─연계사업→ 농식품부 바이오사업, 과기부 바이오헬스
      ─요구TRL→ TRL3~4 (기획), TRL5~6 (본연구)

  분야: 악성종양 치료제
      ─필수기술→ [세포치료, 유전자치료, 면역치료, DDS]
      ─유사과거과제→ [2024-농축-015, 2023-농축-042]
      ─과거선정기관→ [서울대 수의대, A바이오텍, ...]
      ─경쟁강도→ 상 (평균 5:1)
      ─성공패턴→ "PoC 데이터 보유 + 임상 경험 기관이 선정"
```

#### 구현 방법: 점진적 지식 그래프 구축

```sql
-- 1. 사업-기관 관계 (과거 선정 이력 기반)
CREATE TABLE program_institution_history (
  id              SERIAL PRIMARY KEY,
  program_series  TEXT,           -- 사업 시리즈 ID (연도 무관)
  institution_id  FK,
  year            INTEGER,
  role            TEXT,           -- '주관', '공동', '위탁'
  was_selected    BOOLEAN,
  project_title   TEXT,
  outcome_score   DECIMAL,       -- 최종 성과 점수 (추후 추가)
  created_at      TIMESTAMPTZ
);

-- 2. 기술 분류 체계 (Taxonomy)
CREATE TABLE technology_taxonomy (
  id              SERIAL PRIMARY KEY,
  name            TEXT,           -- '세포치료'
  parent_id       FK SELF,       -- '바이오치료' → '세포치료'
  level           INTEGER,       -- 1: 대분류, 2: 중분류, 3: 소분류
  synonyms        TEXT[],         -- {'cell therapy','세포치료제','세포기반치료'}
  related_techs   INTEGER[],     -- 관련 기술 ID 목록
  nrf_code        TEXT,           -- 국가과학기술표준분류체계 코드
  demand_trend    JSONB           -- {"2023": 45, "2024": 62, "2025": 78} 공고 수 추이
);

-- 3. 사업 간 연계 관계
CREATE TABLE program_relationships (
  id              SERIAL PRIMARY KEY,
  source_program  FK,
  target_program  FK,
  relationship    TEXT,   -- 'prerequisite', 'complementary', 'competing', 'successor'
  confidence      DECIMAL -- 0~1 (자동 추론 신뢰도)
);
```

#### 왜 이것이 경쟁우위인가?

| 측면 | 설명 |
|------|------|
| **축적 시간** | 과거 5~10년 공고 + 선정 이력을 구조화하는 것은 6개월+ 소요 |
| **도메인 전문성** | 한국 R&D 생태계의 "암묵지"를 코드화 (예: "농식품부는 산학연 컨소시엄을 선호") |
| **지속적 업데이트** | 매 공고/선정 결과가 그래프를 풍부하게 만듦 (복리 효과) |
| **경쟁자의 난관** | 텍스트는 복사 가능하지만, **관계와 맥락**은 복사할 수 없음 |

### ★ Insight
- Google의 Knowledge Graph가 검색에서 차별화를 만들었듯이, Connect의 **R&D Knowledge Graph**가 매칭에서 차별화를 만듭니다. "세포치료"를 검색하면 키워드 매칭은 해당 단어가 있는 공고만 보여주지만, 지식 그래프는 "유전자치료", "면역치료", "엑소좀" 관련 공고도 **관련성 가중치와 함께** 추천합니다.
- **국가과학기술표준분류체계(NRF 분류)**와 Connect의 자체 분류를 매핑하면, 정부의 공식 분류 체계를 활용하면서도 더 세밀한 매칭이 가능합니다.

---

### 🛡️ Layer 3: 독점 성과 데이터 + 피드백 루프 — **핵심 Moat**

**이것이 Netflix/YouTube의 진짜 경쟁우위이며, Connect의 핵심 Moat가 되어야 합니다.**

#### 핵심 원리: "Outcome Data Flywheel (성과 데이터 플라이휠)"

```
           ┌──────────────────────────────┐
           │                              │
           ▼                              │
    [사용자가 공고를 봄]                   │
           │                              │
           ▼                              │
    [행동 신호 수집]                       │
    - 클릭 / 스킵                         │
    - 저장 / 무시                         │
    - 지원 / 미지원                       │
           │                              │
           ▼                              │
    [지원 결과 추적]          ┌────────────┤
    - 선정 / 탈락             │            │
    - 탈락 사유              │            │
    - 최종 성과              │            │
           │                 │            │
           ▼                 │            │
    [매칭 알고리즘 학습]──────┘            │
    - 성공 패턴 발견                       │
    - 실패 패턴 회피                       │
    - 개인화 정밀도 향상                   │
           │                              │
           ▼                              │
    [더 정확한 추천] ─────────────────────┘
```

#### 수집해야 할 **독점 데이터** 6가지:

```
1. [Implicit Feedback] 암묵적 행동 데이터
   - 공고 상세 페이지 체류 시간
   - 스크롤 깊이 (어디까지 읽었는가)
   - 어떤 섹션을 집중적으로 봤는가 (예산? 자격요건? 연구내용?)
   - 재방문 횟수

2. [Explicit Feedback] 명시적 피드백
   - "이 공고가 맞지 않는 이유" 선택 (예산 부족 / 분야 불일치 / 일정 불가)
   - 매칭 점수에 대한 동의/비동의
   - "비슷한 공고 더 보기" 클릭

3. [Application Outcome] 지원 결과 데이터 ← 🏆 가장 가치 있는 데이터
   - 지원 여부
   - 선정/탈락 결과
   - 탈락 시 추정 사유
   - 최종 수주 금액 (예산 대비 실제 배정)

4. [User Capability Signals] 사용자 역량 신호
   - 과거 수행 과제 이력
   - 보유 특허/논문/기술 목록
   - 연구 장비/시설 보유 현황
   - 팀 구성 (인력 규모, 전문 분야)

5. [Consortium Patterns] 컨소시엄 패턴 데이터
   - 어떤 기관 조합이 선정률이 높은가
   - 주관-공동-위탁 최적 구성 패턴
   - 연구책임자의 과제 수행 이력과 선정률 상관관계

6. [Temporal Patterns] 시계열 패턴
   - 부처별/사업별 공고 주기
   - 예산 증감 추이
   - 신규 사업 출현 패턴
```

#### 왜 이 데이터가 "난공불락"인가?

| 경쟁자의 상황 | Connect의 상황 |
|-------------|--------------|
| Day 1: 사용자 0명, 결과 데이터 0건 | 축적된 수만 건의 행동 로그 + 수천 건의 지원 결과 |
| 매칭 정확도: 키워드 기반 ~30% | 성과 기반 학습으로 ~70%+ |
| 사용자 이탈: "정확도 낮아서 안 씀" | 사용자 유지: "쓸수록 정확해짐" |
| **Cold Start 문제에 갇힘** | **데이터 플라이휠 회전 중** |

#### 구현 우선순위:

> ⚠️ **코드베이스 현황 업데이트 (2026-01-28 검증)**
> 아래 Phase 1, 3, 4는 코드 구현 완료 상태이나, **매칭 API에 미연결(Built but Dormant)**입니다.
> `/api/matches/generate/route.ts`의 `generateMatches()`가 베이스 알고리즘만 호출하며,
> `generatePersonalizedMatches()`는 정의만 되어 있고 프로덕션에서 호출되지 않습니다.

```
Phase 1 (즉시): 행동 로그 수집 인프라 구축
  ✅ 구현 완료 — recommendation_events 테이블 (7개 이벤트 타입)
  ✅ 구현 완료 — /api/events/track 엔드포인트 (Redis 레이트 리밋, 배치 처리)
  ✅ 구현 완료 — 프론트엔드 useMatchTracking 훅 (ImpressionTracker, ViewTracker)
  ✅ 설계 결정 — 파티셔닝 없이 PostgreSQL + 180일 보존 + 30일 학습 윈도우
  ❌ 미완료 — 강한 라벨 이벤트 부재 (APPLIED, NOT_ELIGIBLE, PLANNING 등)

Phase 2 (다음 단계): 지원 결과 추적 시스템
  ❌ 미구현 — Outcome 테이블/API 없음
  - 사용자가 지원 결과를 입력하는 UX 필요
  - 인센티브: 결과 입력 시 → 다음 매칭 정확도 상승 + 프리미엄 기능 일시 해제
  - 게이밍 방지: confidence_level 필드 + 정정 UX + 시간지연 반영

Phase 3 (6개월): 학습 알고리즘 적용
  ✅ 구현 완료 — behavioral-scorer.ts (행동 기반 스코어링)
  ✅ 구현 완료 — item-item-cf.ts (Item-Item Collaborative Filtering)
  ✅ 구현 완료 — contextual-scorer.ts (마감/신선도/트렌딩)
  ✅ 구현 완료 — exploration.ts (epsilon-greedy 탐색)
  ✅ 구현 완료 — preference-aggregator.ts (30일 선호도 집계)
  ✅ 구현 완료 — personalization-layer.ts (통합 오케스트레이션)
  ✅ 구현 완료 — Cold Start 3단계 분류 (FULL_COLD → PARTIAL_COLD → WARM)
  ❌ 핵심 갭 — 매칭 API에 개인화 레이어 미연결 (generatePersonalizedMatches 미호출)

Phase 4 (12개월): 자동 피드백 루프 완성
  ✅ 구현 완료 — interleaving.ts (Team Draft A/B 테스트)
  ✅ 구현 완료 — personalization_config 테이블 (가중치, 트래픽 비율)
  ✅ 구현 완료 — personalization_metrics 테이블 (일별 집계)
  ✅ 구현 완료 — 크론 작업 스케줄러 (4/5/6 AM UTC)
  ⚠️ 주의 — 크론 작업이 스크래퍼 서비스에 의존 (독립 실행 불가)
  ❌ 미완료 — 선정률 예측 모델
```

#### ~~🔴 긴급 실행 과제: "Built but Dormant" 해소~~ → ✅ 해소 완료 (2026-01-28)

```
[이전] 끊김 상태:
  이벤트 수집 ✅ → 선호도 집계 ⚠️(크론 의존) → 개인화 스코어링 ✅
  → 매칭 API 통합 ❌ (여기서 끊김!)

[현재] 연결 완료 (+ 후속 핫픽스 3건 반영):
  이벤트 수집 ✅ → 선호도 집계 ⚠️(크론 의존) → 개인화 스코어링 ✅
  → 매칭 API 통합 ✅ → personalizedScore DB 저장 ✅ → 프론트엔드 표시 ✅
  → 정렬: base score 기준 유지 (personalizedScore 정렬은 500 에러로 리버트)

완료된 작업:
  ✅ 1. /api/matches/generate/route.ts에 generatePersonalizedMatches() 호출 통합
  ✅ 2. personalization_config 시드 데이터 프로덕션 적용 (isActive: true)
  ⚠️ 3. 크론 작업의 스크래퍼 서비스 의존성 제거 (독립 스케줄러) — 아직 미완료
  ✅ 4. on/off 토글: config-loader.ts → personalization_config.isActive DB 조회
  ✅ 5. Redis 캐시 날짜 필드 핫픽스 (contextual-scorer.ts, personalization-layer.ts)
  ✅ 6. CI concurrency group 추가 (병렬 배포 충돌 방지)
```

### ★ Insight
- **YouTube가 "좋아요"보다 "시청 시간"을 중시하는 이유**: 명시적 피드백(좋아요)은 조작 가능하지만, 암묵적 피드백(시청 시간)은 진짜 관심을 반영합니다. Connect에서도 "저장" 버튼보다 **"공고 상세 체류 시간 + 자격요건 섹션 재방문 횟수"**가 더 정확한 관심 신호입니다.
- **"지원 결과 데이터"는 Connect의 핵(核)입니다.** 이 데이터가 1,000건 축적되면 "이 프로필의 기업이 이 유형의 사업에 지원했을 때 선정 확률 X%"라는 **예측**이 가능해집니다. 경쟁자가 이것을 따라잡으려면 Connect와 동일한 시간이 필요합니다.

---

### 🔒 Layer 4: 네트워크 효과 + 전환비용 — **궁극적 방어벽**

#### 네트워크 효과 (Network Effects):

```
현재 Connect: 단방향 (공고 → 기업)
              기업이 공고를 검색하는 "검색 엔진" 수준

미래 Connect: 양방향 네트워크
              기업 ←→ 연구기관 ←→ 정부사업

┌─────────────────────────────────────────────┐
│                                             │
│   [기업 A]──지원──→[과제 X]←──공동연구──[대학 B] │
│      │                                  │   │
│      └──────컨소시엄 매칭────────────────┘   │
│                                             │
│   기업이 많을수록 → 연구기관이 모이고          │
│   연구기관이 많을수록 → 기업이 모이고          │
│   양쪽이 많을수록 → 매칭 품질 상승            │
│                                             │
│   = 자기강화 루프 (Self-reinforcing Loop)     │
└─────────────────────────────────────────────┘
```

#### 전환비용 (Switching Cost):

```
사용자가 Connect를 떠나기 어려운 이유를 설계:

1. [데이터 축적] 3년간의 지원 이력 + 성과 데이터 → 다른 서비스에서 재구축 불가
2. [개인화 학습] 사용할수록 정밀해지는 매칭 → 새 서비스는 다시 Cold Start
3. [관계 네트워크] Connect에서 형성된 기관-기업 파트너십 → 이전 불가
4. [워크플로우 통합] 공고 발견 → 지원서 작성 → 결과 추적 → 성과 관리 일원화
```

---

## 📋 3단계: 실행 로드맵 (우선순위별)

> ⚠️ **2026-01-28 업데이트:** 코드베이스 검증 결과, 기존 로드맵의 상당 부분이 이미 구현됨.
> 아래 테이블은 **미완료 갭 중심**으로 재조정한 실행 로드맵입니다.
>
> **🟢 2026-01-28 세션 2 완료:** Tasks 1~3 프로덕션 연결 완료. Layer 3 "독점 성과 데이터 피드백 루프" 기초 구축.

| 순서 | 과제 | 현재 상태 | 목적 | 방어벽 기여 |
|------|------|:--------:|------|-----------|
| ~~**1**~~ | 개인화 레이어 프로덕션 연결 | ✅ **완료** (2026-01-28) | `generatePersonalizedMatches()` → `/api/matches/generate` 통합, `personalizedScore` 필드, config-loader 런타임 토글. ⚠️ personalizedScore 정렬은 500 에러로 리버트됨 → base score 정렬 유지 중 | Layer 3 **활성화** |
| ~~**2**~~ | 강한 라벨 이벤트 확장 (APPLIED/NOT_ELIGIBLE/PLANNING) | ✅ **완료** (2026-01-28) | 3개 이벤트 타입 + metadata + client/server 검증 + UI 버튼 + 가중치(APPLIED:2.0, PLANNING:0.8, NOT_ELIGIBLE:-0.2) | Layer 3 강화 |
| ~~**3**~~ | 지원 결과 추적 UX + 인센티브 + 게이밍 방지 설계 | ✅ **완료** (2026-01-28) | `application_outcomes` 테이블 + `/api/outcomes` + 상태 전이 검증 + 게이밍 방지(confidence 가중치 + 7일 지연) + analytics API | Layer 3 핵심 |
| **4** | 사업 시리즈 매핑 (연도 무관 동일 사업 식별) | ❌ 미구현 | 과거 데이터 활용 기반, 추천 설명 품질 향상 | Layer 2 |
| **5** | 하드필터 추출 품질 집중 개선 (기업참여필수/마감시간/예산/자격요건) | 🟡 부분 구현 | 즉시 체감 매칭 정확도 향상 | Layer 1 강화 |
| **6** | 3-Tier PDF 추출 파이프라인 | ❌ 미구현 | Layer 1 비용 최적화 | Layer 1 |
| **7** | 컨소시엄 매칭 기능 (기관↔기업) | 🟡 partner-algorithm.ts 존재 | 양방향 네트워크 시작 | Layer 4 |
| **8** | 선정률 예측 모델 | ❌ 미구현 | Outcome 데이터 기반 예측 (Task 3 완료로 데이터 수집 개시됨) | Layer 3 가속 |

#### 완료된 항목 (기존 로드맵에서 제거):

| 기존 순서 | 과제 | 현재 상태 | 구현 위치 |
|:---------:|------|:--------:|----------|
| ~~1~~ | 행동 로그 수집 인프라 | ✅ **완료** | `recommendation_events` + `/api/events/track` + `useMatchTracking` |
| ~~3~~ | 기술 분류 체계(Taxonomy) | ✅ **완료** | `lib/matching/taxonomy.ts` (623줄, 계층구조+동의어+교차산업 매트릭스) |
| ~~6~~ | Collaborative Filtering v1 | ✅ **완료** | `lib/personalization/item-item-cf.ts` + `program_co_occurrence` 테이블 |
| ~~8~~ | A/B 테스트 인프라 | ✅ **완료** | `lib/personalization/interleaving.ts` + `personalization_config` 테이블 |

---

## 🎯 핵심 결론

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  PDF 추출은 "입장료"이지 "경쟁우위"가 아닙니다.            │
│                                                          │
│  Connect의 진짜 경쟁우위는:                               │
│                                                          │
│  1. 경쟁자가 "시간으로만" 따라잡을 수 있는                 │
│     독점 성과 데이터의 축적                                │
│                                                          │
│  2. 그 데이터가 매일 알고리즘을 개선하는                   │
│     자기강화 피드백 루프                                   │
│                                                          │
│  3. 사용자가 떠날 수 없는                                 │
│     네트워크 효과와 전환비용                               │
│                                                          │
│  [2026-01-28 세션 2 완료 + 후속 핫픽스]                    │
│  ✅ 개인화 레이어 프로덕션 연결 (Built→Active)             │
│  ✅ 강한 라벨 이벤트 (APPLIED/NOT_ELIGIBLE/PLANNING)       │
│  ✅ 지원 결과 추적 + 게이밍 방지 + Analytics API          │
│  ✅ 핫픽스: Redis 날짜, CI 동시성, 정렬 리버트            │
│                                                          │
│  ⚠️ 보류:                                                 │
│  → personalizedScore 정렬 재활성화 (크론 실행 후 재시도)   │
│                                                          │
│  다음 단계:                                               │
│  → ④ 사업 시리즈 매핑 (연도 무관 동일 사업 식별)           │
│  → ⑤ 하드필터 추출 품질 개선                              │
│  → ⑧ 선정률 예측 모델 (Outcome 데이터 수집 개시됨)        │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### ★ Insight
- **"데이터가 없는 AI는 엔진 없는 자동차"**입니다. Connect가 지금 투자해야 할 것은 더 나은 PDF 파서가 아니라, **사용자 행동과 지원 결과를 빠짐없이 수집하는 인프라**입니다.
- Netflix가 2000년대 초반에 한 일이 정확히 이것이었습니다: DVD 배송 시절부터 모든 사용자 행동을 기록하여, 스트리밍 시대에 추천 시스템의 기반 데이터로 활용했습니다.
- **첫 1,000건의 지원 결과 데이터가 쌓이는 순간, Connect는 경쟁자 대비 1,000건 × 학습 = 돌이킬 수 없는 격차**를 갖게 됩니다.
- **[2026-01-28 추가]** "구현됨(Built)"과 "활성화됨(Active)"은 다릅니다. ~~Connect는 6-Phase 개인화 아키텍처를 구현했지만, 매칭 API와의 연결이 누락되어 프로덕션에서 베이스 알고리즘만 작동 중입니다.~~ **→ 2026-01-28 세션 2에서 해결 완료.** `generatePersonalizedMatches()`가 매칭 파이프라인에 통합되었으며, config-loader를 통한 런타임 on/off가 가능합니다.
- **[2026-01-28 세션 2 완료]** Layer 3 "독점 성과 데이터 피드백 루프"의 기초 인프라가 완성되었습니다:
  - **개인화 파이프라인 활성화**: `personalizedScore` 필드 + `generatePersonalizedMatches()` 통합 (graceful degradation 포함)
  - **강한 라벨 이벤트**: APPLIED(2.0), PLANNING(0.8), NOT_ELIGIBLE(-0.2) 가중치로 학습 신호 품질 대폭 향상
  - **지원 결과 추적**: `application_outcomes` 테이블 + 상태 전이 검증 + 게이밍 방지(confidence 가중치 + 7일 지연)
  - **핵심 파일**: `config-loader.ts`(신규), `generate/route.ts`(통합), `outcomes/route.ts`(신규), `OutcomeInputPrompt.tsx`(신규)
  - **다음 단계**: 첫 1,000건의 APPLIED/SELECTED/REJECTED 데이터가 축적되면 선정률 예측 모델(Task 8) 구축 가능
- **[후속 핫픽스 3건]** 프로덕션 연결 직후 발견된 이슈 즉시 해결:
  1. **personalizedScore 정렬 리버트** (`4fa5e7a`): `orderBy: personalizedScore desc nulls last` 적용 시 500 에러 → base score 정렬로 리버트. personalizedScore는 DB 저장 + API 응답 포함되지만, 정렬은 base score 기준 유지. 원인: 크론 미실행으로 personalizedScore가 대부분 null인 상태에서 null 정렬 처리 이슈 추정.
  2. **Redis 캐시 날짜 필드** (`4cf3c5b`): Redis 역직렬화 시 `Date` 객체가 ISO 문자열로 반환되어 `.getTime()` 호출 시 TypeError → `new Date()` 래핑으로 해결 (`contextual-scorer.ts` 4개 지점, `personalization-layer.ts` 1개 지점)
  3. **CI 동시 배포 방지** (`aad4546`): GitHub Actions에 concurrency group 추가하여 동시 배포 시 이전 실행 자동 취소
- **교훈**: 새 파이프라인 프로덕션 연결 시 "Happy Path 테스트 통과 ≠ 프로덕션 안정". Redis 직렬화 불일치, null 데이터 정렬 등은 통합 테스트에서만 발견됨. Netflix/YouTube에서도 신규 추천 파이프라인 연결 후 48시간 내 핫픽스 2~5건은 정상 범위.

---

## 📊 부록: 코드베이스 현황 스냅샷 (2026-01-28)

> 전략 문서 작성 당시 가정했던 "미구현" 상태와 실제 코드베이스 간의 괴리를 기록합니다.

### 매칭 알고리즘 (v4.2.2)

| 컴포넌트 | 파일 | 상태 |
|----------|------|:----:|
| 베이스 매칭 알고리즘 (6개 점수 + 10+ 하드필터) | `lib/matching/algorithm.ts` (1,003줄) | ✅ 프로덕션 |
| 결정론적 키워드 분류기 (450+ NTIS 타이틀) | `lib/matching/keyword-classifier.ts` (627줄) | ✅ 프로덕션 |
| 산업 분류 체계 (계층+동의어+교차산업) | `lib/matching/taxonomy.ts` (623줄) | ✅ 프로덕션 |
| TRL 호환성 스코어링 | `lib/matching/trl.ts` (317줄) | ✅ 프로덕션 |
| 3단계 자격 검증 (FULLY/CONDITIONALLY/INELIGIBLE) | `lib/matching/eligibility.ts` (417줄) | ✅ 프로덕션 |
| 한국어 매칭 설명 생성 | `lib/matching/explainer.ts` (463줄) | ✅ 프로덕션 |
| SME24 별도 매칭 | `lib/matching/sme-algorithm.ts` (1,079줄) | ✅ 프로덕션 |
| 컨소시엄 파트너 매칭 | `lib/matching/partner-algorithm.ts` (633줄) | ✅ 프로덕션 |

### 개인화 인프라 (✅ Active — 2026-01-28 프로덕션 연결 완료)

| 컴포넌트 | 파일 | 구현 | API 연결 |
|----------|------|:----:|:-------:|
| 이벤트 추적 API (10종 이벤트) | `/api/events/track/route.ts` | ✅ | ✅ |
| 프론트엔드 추적 훅 (7+3 이벤트) | `hooks/useMatchTracking.ts` | ✅ | ✅ |
| 서버사이드 이벤트 로거 | `lib/personalization/event-logger.ts` | ✅ | ✅ |
| 클라이언트 트래커 (APPLIED/NOT_ELIGIBLE/PLANNING 포함) | `lib/personalization/client-tracker.ts` | ✅ | ✅ |
| 뷰 트래커 (IntersectionObserver) | `lib/personalization/view-tracker.ts` | ✅ | ✅ |
| 세션 관리자 | `lib/personalization/session-manager.ts` | ✅ | ✅ |
| 선호도 집계기 (10종 가중치) | `lib/personalization/preference-aggregator.ts` | ✅ | ⚠️ 크론 의존 |
| 행동 스코어러 | `lib/personalization/behavioral-scorer.ts` | ✅ | ✅ (via personalization-layer) |
| Item-Item CF | `lib/personalization/item-item-cf.ts` | ✅ | ✅ (via personalization-layer) |
| 컨텍스트 스코어러 | `lib/personalization/contextual-scorer.ts` | ✅ | ✅ (via personalization-layer) |
| 탐색 모듈 (epsilon-greedy) | `lib/personalization/exploration.ts` | ✅ | ✅ (via personalization-layer) |
| **개인화 레이어 (오케스트레이터)** | `lib/personalization/personalization-layer.ts` | ✅ | ✅ **프로덕션 연결** |
| **설정 로더 (런타임 토글)** | `lib/personalization/config-loader.ts` | ✅ | ✅ **(신규)** |
| A/B 인터리빙 | `lib/personalization/interleaving.ts` | ✅ | ✅ (via personalization-layer) |
| 메트릭 수집기 | `lib/personalization/metrics-collector.ts` | ✅ | ✅ (via personalization-layer) |
| 크론 스케줄러 | `lib/personalization/cron.ts` | ✅ | ⚠️ 스크래퍼 의존 |

### 데이터베이스 모델 (Prisma)

| 테이블 | 용도 | 상태 |
|--------|------|:----:|
| `recommendation_events` | 사용자 행동 이벤트 (10종, metadata 포함) | ✅ 수집 중 |
| `organization_preferences_derived` | 30일 선호도 집계 | ⚠️ 크론 의존 |
| `organization_personalization_status` | Cold Start 단계 | ⚠️ 크론 의존 |
| `program_co_occurrence` | 프로그램 유사도 | ⚠️ 크론 의존 |
| `organization_neighbors` | 조직 유사도 | ⚠️ 크론 의존 |
| `personalization_config` | A/B 테스트 설정 | ✅ 시드 완료 (active) |
| `personalization_metrics` | 일별 메트릭 집계 | ⚠️ 크론 의존 |
| `funding_matches.personalizedScore` | 개인화 점수 (SmallInt?) | ✅ **(신규)** |
| `application_outcomes` | 지원 결과 추적 (7상태 + 게이밍방지) | ✅ **(신규)** |

### API 가격 기준 (2026-01-28 공식 확인)

| 모델 | Input / MTok | Output / MTok | 비고 |
|------|:-----------:|:------------:|------|
| Claude Opus 4.5 | $5 | $25 | 현재 최신 |
| Claude Opus 4/4.1 (Legacy) | $15 | $75 | 레거시 |
| Claude Sonnet 4.5 | $3 | $15 | ≤200K context |
| Claude Haiku 4.5 | $1 | $5 | 3-Tier Tier 2 후보 |
